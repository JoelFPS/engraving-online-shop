username
adres
totalPrice
productName 
{
    quantity (of product)
    details (of product) like color
}

Przesyłanie żądania z frontu do backendu:
https://stackabuse.com/get-http-request-in-react/ - w reakcie

view/productPage:
Na podstawie przesłanego id szuka w bazie odpowiednich danych i wstawia je w odpowiednich miejscach do szablonu na tej stronie. 

Admin: 
* wprowadzanie produktów do bazy ^
* 

User:
* strona główna - wczytanie domyślnych produktów na stronę główną
* strona produktu - wczytanie z bazy odpowiedniego produktu po id - metoda GET

    /*this.useState ({
        productData: null
    })*/

function getProduct() {
    console.log("getProduct()");
    const requestOptions = {
        method: 'GET',
        headers: { 'Content-Type': 'application/json' },
    };

    const tableId = [
        "656db87f6969bb7bd4bf73ef",
        "656dbb3f6969bb7bd4bf73f1",
        "656dbb536969bb7bd4bf73f3",
        "656dbb6e6969bb7bd4bf73f5"
    ];

    fetch(`http://localhost:5001/api/products/${tableId[1]}`, requestOptions)
        .then(response => response.json())
        .then(data => {
            setProductData(
                [data]
            );
        })
        .catch(error => {
            console.error('Error fetching product:', error);
        });
}

//getProduct();